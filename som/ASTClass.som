ASTClass = (
  | name extends fields methods |

  name:    v = ( name    := v )
  extends: v = ( extends := v )

  asString = (
    ^ 'ASTClass name: ' + name + ' extends: ' + extends
  )

----
  new: n = ( | c |
    c := ASTClass new.
    c name: n.
    c extends: 'Object'.
    ^c
  )
)
